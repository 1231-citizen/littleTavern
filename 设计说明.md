# 小酒馆 · 设计说明

> 类酒馆（SillyTavern-like）移动端应用 · Flutter / Android · 日系清新风
> 代码位置：`app/`　构建产物：`app/build/app/outputs/flutter-apk/app-debug.apk`
> 当前版本：v1.1（第一版的 8 条更改要求见文末「六」）

---

## 一、需求映射表

### 1.1 `要求.txt`

| 要求 | 实现 | 代码位置 |
|---|---|---|
| 类酒馆软件 | 角色卡驱动的单角色对话应用，一个角色卡 = 一条独立会话 | `lib/store.dart` |
| 支持导入 DeepSeek 的 API 进行对话 | API 设置页支持自定义接口地址 / Key / 模型，**原生直连无 CORS 问题**，流式 SSE 逐字输出 | `lib/api/deepseek.dart`、`lib/screens/api_screen.dart` |
| 主要面向手机端 | Flutter Android 原生 App，手机尺寸布局，触控优先（长按 / 点击 / 底部弹栏） | 全局 |
| 两个角色：用户 + AI 角色 | `MsgRole.user` / `MsgRole.assistant`，用户设定是全局的，AI 角色来自角色卡 | `lib/models.dart` |
| **AI 角色必须先设定角色卡才能对话** | 无角色卡时输入框禁用、主区展示引导页，只有「创建角色卡」一条路 | `lib/screens/chat_screen.dart` |
| 角色卡三要素：人物名称 / 人物设定 / 人物外在形象 | 角色卡编辑页三个字段，缺名称不允许保存；三要素全部注入 system prompt | `lib/screens/character_edit_screen.dart` |

### 1.2 `具体布局.txt`

| 要求 | 实现 |
|---|---|
| 下侧方框，左侧输入文字，右侧按钮发送 | `Composer`：白色圆角方框 + 发丝级描边；左多行输入、右 40×40 发送键；生成中变为「停止」键 |
| 上面展示对话 | 主区 `ListView` 对话流 |
| 对话紧凑 | 气泡内边距 10/9/13/11，同角色相邻气泡间距 6px，异角色 14px |
| 对话框包含左上侧人物形象 | 头像固定在气泡**内部左上角**（30px 圆形，发丝级描边），用户与 AI 一致 |
| 点击人物形象修改人物设定 | AI 气泡 → 该角色卡编辑页；用户气泡 → 全局用户设定页 |
| 点击对话框修改内容 | 弹出多行编辑器，提示「保存后这条之后的 N 条会被清除」，保存即截断后续消息；**改的是用户发言时，保存后立刻用这条发言自动续上对话**，改角色发言则不触发（见 `_editMessage`） |
| 长按弹出**适中**的栏 | 底部弹栏（`maxHeightFactor: 0.6`），含：复制内容 / 修改内容 / 修改思维链 / 显示·隐藏思维链 / 删除这条对话 |
| 删除时同理往后清除 | `deleteMessageAt(index)` → `removeRange(index, length)` |
| 是否支持思维链返回 | 每气泡独立开关（头部「思维链 · 开 / 思维链」小标签），长按菜单亦可切换；思维链以薄荷色块展示在正文上方，**色块右上角的铅笔或长按菜单都能直接改思维链** |
| 复制的功能 | 长按菜单「复制内容」→ 系统剪贴板 + toast |

### 1.3 左上角「栏」

| 栏目项 | 实现 |
|---|---|
| 世界背景设定 | **多张世界卡**（名字 + 正文），点击即切换，切换同时作用于当前这段对话；每段历史对话各自记得自己用的世界。注入 system prompt 的「世界背景」段 |
| API 接入入口（调整不同模型 / 设置推理等级） | 模型下拉（deepseek-chat / deepseek-reasoner / 自定义）+ 推理等级四档（关闭 / 低 / 中 / 高）+ 温度 / 最大长度 / 流式开关 + 附加 JSON 参数 + **测试连接** |
| 全局用户设定 | **多套用户设定**，同样是「点击即切换、铅笔编辑、长按删除」；名称 + 设定 + 形象 + 色调，跨角色卡复用 |
| 酒馆人物管理 | 角色卡列表，点击即切换到这个人物、铅笔进编辑页、长按删除、底部创建新卡；当前角色带「当前」标记 |
| 栏目排序 | **长按任意一行即可拖动改变四个栏目的先后次序**，顺序写入本机存储（`drawerOrder`） |

---

## 二、信息架构

```
ChatScreen（对话主界面）
├── 顶栏
│   ├── 左：☰ ──→ AppDrawer（栏，长按可拖动排序）
│   │              ├── 世界背景设定 ──→ WorldScreen（世界卡列表）──→ WorldEditScreen
│   │              ├── API 接入 ──────→ ApiScreen
│   │              ├── 全局用户设定 ──→ UserScreen（用户设定列表）──→ UserEditScreen
│   │              └── 酒馆人物管理 ──→ CharacterListScreen ──→ CharacterEditScreen
│   ├── 中：角色名 + 当前世界名
│   └── 右：🕘 ──→ 开启新对话 / 编辑当前角色卡 / 当前人物的历史对话
├── 对话流 MessageBubble × N
│   ├── 点头像 → 人物设定
│   ├── 点气泡 → 改内容（截断后续；用户发言改完自动续聊）
│   └── 长按   → 复制 / 修改 / 改思维链 / 思维链开关 / 删除（截断后续）
└── Composer（下侧方框）
```

一个角色可以有多段对话：右上角「开启新对话」时，原来那段自动留在该角色的历史对话里，
随时可以从历史对话里切回去。

---

## 三、数据模型

```dart
CharacterCard { id, name, persona, appearance, avatar, avatarImage?, colorIndex, createdAt }
ChatMessage   { id, role, content, reasoning, showReasoning, at, characterId, error }
ChatSession   { id, characterId, worldId?, messages[], createdAt, updatedAt }  // 一卡可多会话
UserProfile   { id, name, persona, avatar, avatarImage?, colorIndex, createdAt } // 可有多套
WorldCard     { id, name, content, createdAt }                                  // 可有多张
ApiConfig     { baseUrl, apiKey, model, reasoning, sendReasoningEffort,
                temperature, maxTokens, stream, extraJson }
```

持久化：`shared_preferences`（JSON 字符串）。Key 前缀 `tavern.*`，
会话与用户设定升到 v2/v1 新键（`tavern.sessions.v2` / `tavern.users.v1` / `tavern.worlds.v1`），
旧数据在 `load()` 里自动迁移：旧会话里的 `world` 字符串会升级成一张名叫「默认世界」的世界卡。
**API Key 只写入本机**，不经过任何中转服务器。

---

## 四、提示词组装

```
system:
  你正在进行沉浸式角色扮演，请始终以指定角色的身份说话。
  【世界背景】 ← 会话级
  【你扮演的角色】名称 / 人物设定 / 外在形象 ← 角色卡三要素
  【对话者（用户）】名称 / 设定 ← 全局用户设定
  【扮演要求】第一人称、动作入括注、不代替用户行动、不输出元信息…
history:
  user / assistant 正文（**思维链不回传**，符合 DeepSeek 对 reasoning_content 的要求）
```

---

## 五、API 接入方案

- 通用 **OpenAI 兼容**客户端，只用 `dart:io`，零第三方依赖。
- 地址容错：`https://api.deepseek.com` / `/` / `/v1` 均自动拼成 `…/chat/completions`。
- 流式解析 SSE：`data: {...}` 增量读取 `delta.content` 与 `delta.reasoning_content`（兼容 `delta.reasoning` 与分段数组 content）。
- 非流式回退：`choices[0].message`。
- 终止生成：取消订阅 + 关闭连接；若尚无内容则撤回气泡。
- 错误呈现：HTTP 状态码 / 接口 error 对象 / 网络异常，都渲染为气泡内的粉色提示块，不弹系统对话框。
- 推理等级：默认不发送 `reasoning_effort`（部分兼容接口会 400），需要时在设置里显式打开；`deepseek-reasoner` 的思维链自动解析展示。

---

## 六、v1.1 更改记录（针对第一版的 8 条要求）

要求原件：`更改要求/对第一版的更改要求.txt`

| # | 要求 | 落地 |
|---|---|---|
| 1 | 角色卡人物形象：emoji 减到五个，并能加照片当头像 | `kEmojiSet5`（🌸🍃🌙🍵🕊️）替换原来一排 24 个；新增「从相册选择照片 / 换一张 / 移除照片」。照片走原生相册选择器（`MainActivity.kt` 的 `tavern/image` 通道 + `PickVisualMedia`），原生侧压到 640px 存进应用私有目录，Dart 只存路径，**不需要任何存储权限、不引入第三方依赖** |
| 2 | 删除角色卡编辑页的「色调」 | 角色卡编辑页去掉色调区块（`CharacterCard.colorIndex` 保留兼容旧数据，不再暴露） |
| 3 | 思维链也要能改 | 思维链色块右上角新增铅笔，长按菜单新增「修改思维链」→ `AppStore.editReasoningAt()`。思维链本来就不回传给模型，所以只就地改写，不触发重新对话 |
| 4 | 改用户发言后自动续聊，改 AI 发言不触发 | `_editMessage()` 按 `m.role` 分流：用户发言 → 截断后续并 `replyAfterUserEdit()` 立刻重新请求；角色发言 → 只更新 |
| 5 | 点击对话框的动画加快 | 底部弹栏 500ms → 300ms（`sheetAnimationStyle`，同时是项目风格允许的最短值 300ms）、弹栏内边距动画、对话滚动跟随、气泡容器动画一律改用 `JF.durFast` |
| 6 | 用户设定 / 世界背景改成和人物管理一样的可切换形式 | `UserScreen` / `WorldScreen` 改成列表页（点击即切换、铅笔编辑、长按删除），分别配 `UserEditScreen` / `WorldEditScreen`；新增 `UserProfile.id`、`WorldCard`，持久化到 `tavern.users.v1` / `tavern.worlds.v1` |
| 7 | 侧栏栏目可长按拖动排序 | 抽屉改用 `ReorderableListView` + `ReorderableDelayedDragStartListener`，顺序存 `drawerOrder`（`tavern.drawer.v1`），非法键与缺项自动补齐 |
| 8 | 右上角改为「当前人物的历史对话 + 开启新对话」 | `ChatScreen` 右上角换成 🕘：开启新对话（旧的自动留在历史里）、编辑当前角色卡、该角色的历史对话列表（点击切回、长按删除）。底层 `ChatSession` 从「一卡一会话」升级为「一卡多会话」，`sessionsOf()` 按更新时间倒序 |


