# 小酒馆 · 第一版设计说明

> 类酒馆（SillyTavern-like）移动端应用 · Flutter / Android · 日系清新风
> 代码位置：`app/`　构建产物：`app/build/app/outputs/flutter-apk/app-debug.apk`

---

## 一、需求映射表

### 1.1 `要求.txt`

| 要求 | 实现 | 代码位置 |
|---|---|---|
| 类酒馆软件 | 角色卡驱动的单角色对话应用，一个角色卡 = 一条独立会话 | `lib/store.dart` |
| 支持导入 DeepSeek 的 API 进行对话 | API 设置页支持自定义接口地址 / Key / 模型，**原生直连无 CORS 问题**，流式 SSE 逐字输出 | `lib/api/deepseek.dart`、`lib/screens/api_screen.dart` |
| 主要面向手机端 | Flutter Android 原生 App，手机尺寸布局，触控优先（长按 / 点击 / 底部弹栏） | 全局 |
| 两个角色：用户 + AI 角色 | `MsgRole.user` / `MsgRole.assistant`，用户设定是全局的，AI 角色来自角色卡 | `lib/models.dart` |
| **AI 角色必须先设定角色卡才能对话** | 无角色卡时输入框禁用、主区展示引导页，只有「创建角色卡」一条路 | `lib/screens/chat_screen.dart` |
| 角色卡三要素：人物名称 / 人物设定 / 人物外在形象 | 角色卡编辑页三个字段，缺名称不允许保存；三要素全部注入 system prompt | `lib/screens/character_edit_screen.dart` |

### 1.2 `具体布局.txt`

| 要求 | 实现 |
|---|---|
| 下侧方框，左侧输入文字，右侧按钮发送 | `Composer`：白色圆角方框 + 发丝级描边；左多行输入、右 40×40 发送键；生成中变为「停止」键 |
| 上面展示对话 | 主区 `ListView` 对话流 |
| 对话紧凑 | 气泡内边距 10/9/13/11，同角色相邻气泡间距 6px，异角色 14px |
| 对话框包含左上侧人物形象 | 头像固定在气泡**内部左上角**（30px 圆形，发丝级描边），用户与 AI 一致 |
| 点击人物形象修改人物设定 | AI 气泡 → 该角色卡编辑页；用户气泡 → 全局用户设定页 |
| 点击对话框修改内容 | 弹出多行编辑器，提示「保存后这条之后的 N 条会被清除」，保存即截断后续消息 |
| 长按弹出**适中**的栏 | 底部弹栏（`maxHeightFactor: 0.52`），含四项：复制内容 / 修改内容 / 显示·隐藏思维链 / 删除这条对话 |
| 删除时同理往后清除 | `deleteMessageAt(index)` → `removeRange(index, length)` |
| 是否支持思维链返回 | 每气泡独立开关（头部「思维链 · 开 / 思维链」小标签），长按菜单亦可切换；思维链以薄荷色块展示在正文上方 |
| 复制的功能 | 长按菜单「复制内容」→ 系统剪贴板 + toast |

### 1.3 左上角「栏」

| 栏目项 | 实现 |
|---|---|
| 世界背景设定 | 当前会话独立存储，注入 system prompt 的「世界背景」段 |
| API 接入入口（调整不同模型 / 设置推理等级） | 模型下拉（deepseek-chat / deepseek-reasoner / 自定义）+ 推理等级四档（关闭 / 低 / 中 / 高）+ 温度 / 最大长度 / 流式开关 + 附加 JSON 参数 + **测试连接** |
| 全局用户设定 | 用户名称 + 用户设定 + 形象 + 色调，跨角色卡复用 |
| 酒馆人物管理 | 角色卡列表，点按编辑、长按删除、底部创建新卡；当前角色带「当前」标记 |

---

## 二、信息架构

```
ChatScreen（对话主界面）
├── 顶栏
│   ├── 左：☰ ──→ AppDrawer（栏）
│   │              ├── 世界背景设定 ──→ WorldScreen
│   │              ├── API 接入 ──────→ ApiScreen
│   │              ├── 全局用户设定 ──→ UserScreen
│   │              └── 酒馆人物管理 ──→ CharacterListScreen ──→ CharacterEditScreen
│   ├── 中：角色名 + 世界背景摘要
│   └── 右：⋯ ──→ 世界背景 / 编辑当前角色卡 / 清空当前对话
├── 对话流 MessageBubble × N
│   ├── 点头像 → 人物设定
│   ├── 点气泡 → 改内容（截断后续）
│   └── 长按   → 复制 / 修改 / 思维链 / 删除（截断后续）
└── Composer（下侧方框）
```

---

## 三、数据模型

```dart
CharacterCard { id, name, persona, appearance, avatar, colorIndex, createdAt }
ChatMessage   { id, role, content, reasoning, showReasoning, at, characterId, error }
ChatSession   { id, characterId, world, messages[], updatedAt }   // 一卡一会话
UserProfile   { name, persona, avatar, colorIndex }               // 全局
ApiConfig     { baseUrl, apiKey, model, reasoning, sendReasoningEffort,
                temperature, maxTokens, stream, extraJson }
```

持久化：`shared_preferences`（JSON 字符串）。Key 前缀 `tavern.*.v1`。
**API Key 只写入本机**，不经过任何中转服务器。

---

## 四、提示词组装

```
system:
  你正在进行沉浸式角色扮演，请始终以指定角色的身份说话。
  【世界背景】 ← 会话级
  【你扮演的角色】名称 / 人物设定 / 外在形象 ← 角色卡三要素
  【对话者（用户）】名称 / 设定 ← 全局用户设定
  【扮演要求】第一人称、动作入括注、不代替用户行动、不输出元信息…
history:
  user / assistant 正文（**思维链不回传**，符合 DeepSeek 对 reasoning_content 的要求）
```

---

## 五、API 接入方案

- 通用 **OpenAI 兼容**客户端，只用 `dart:io`，零第三方依赖。
- 地址容错：`https://api.deepseek.com` / `/` / `/v1` 均自动拼成 `…/chat/completions`。
- 流式解析 SSE：`data: {...}` 增量读取 `delta.content` 与 `delta.reasoning_content`（兼容 `delta.reasoning` 与分段数组 content）。
- 非流式回退：`choices[0].message`。
- 终止生成：取消订阅 + 关闭连接；若尚无内容则撤回气泡。
- 错误呈现：HTTP 状态码 / 接口 error 对象 / 网络异常，都渲染为气泡内的粉色提示块，不弹系统对话框。
- 推理等级：默认不发送 `reasoning_effort`（部分兼容接口会 400），需要时在设置里显式打开；`deepseek-reasoner` 的思维链自动解析展示。

---

## 六、日系清新风落地

| 风格规则 | 落地方式 |
|---|---|
| 只用 light / extralight 字重 | 全局唯一字体 `WenKai`（霞鹜文楷 Light，单字重）；`JF` 里最高只到 `FontWeight.w300` |
| 发丝级边框 | 全部 0.8px；卡片 `#d4d4cf` @30%，按钮 @40%，分隔线 @20% |
| 无可见阴影 | 仅 `0_1px_3px rgba(0,0,0,0.03)` 级别的浮起 |
| 圆角 | 一律 `rounded-xl(16)` / `rounded-lg(12)`，无直角 |
| 不用深色背景 | 米白 `#fafaf8` + 纸白 `#ffffff`，状态栏透明深色图标 |
| 每区块一株植物线描 | `Botanical` CustomPainter，四款（叶枝 / 竹 / 花枝 / 草），按区块各用其一 |
| 底线式输入框 + 浮动标签 | `JF.underline()`，`JFField` 全站统一 |
| 慢动效 | 过渡统一 `Duration(milliseconds: 500)` + `easeInOut`；悬停仅上浮 0.5px；按下改透明度不缩放 |
| **间（Ma）留白** | 二级页 section 间距 30–52px，空状态上下 40–48px |
| 无嵌套卡片 | 抽屉、设置页一律用「区块 + 发丝分隔线」，不做卡中卡 |
| reduced-motion | `MediaQuery.disableAnimations` 时关闭呼吸点动画 |
| 对比度 | 正文 `#4a5568`(≈7.4:1) / `#6b7280`(≈4.8:1)，满足 WCAG AA |

---

## 七、构建与运行（工具链在 E 盘）

工具链位置（不入库）：
```
E:\AndroidDev\flutter         Flutter 3.47.4
E:\AndroidDev\jdk             Temurin JDK 17.0.20.1
E:\AndroidDev\android-sdk     Android SDK 36 + build-tools 36
```
构建缓存留在工作区：`.pub-cache/`、`.gradle/`。

```powershell
# 1) 编译 debug APK
pwsh tools\build-apk.ps1

# 2) 启动雷电模拟器 + 安装 + 截图
pwsh tools\run-on-emulator.ps1
```

国内网络注意（已配置好）：
- GitHub 直连不通 → 字体走 `ghfast.top` 代理，JDK 走清华 Adoptium 镜像
- `maven.google.com` 不通 → `android/settings.gradle.kts` 与 `android/build.gradle.kts` 已优先挂阿里云镜像
- Gradle 发行包 → 换腾讯云镜像 `mirrors.cloud.tencent.com/gradle`

---

## 八、第二版待办

1. 多角色群聊（目前严格按要求只做两人对话）
2. 角色卡导入 / 导出（PNG 卡片、JSON）、从示例卡库导入
3. 头像支持真实图片（当前为 emoji + 色调，避免相册权限）
4. 会话分支树 / 重新生成 / swipe 换回复
5. 世界书（World Info）关键词触发注入
6. 本地正则替换、输出格式约束
7. 对话导出为长图 / Markdown

---

## 九、第一版验收记录

运行环境：雷电模拟器 14 · Android 14 (API 34) · 1080×1920 @440dpi（约 393×698 dp）

| # | 验收项 | 结果 | 截图 |
|---|---|---|---|
| 1 | 对话主界面：紧凑气泡、头像在气泡左上侧、上对话下输入框 | 通过 | `tools/shots/10-chat.png`、`24-scroll.png` |
| 2 | 左上角展开「栏」：世界背景 / API 接入 / 用户设定 / 人物管理 | 通过 | `11-drawer.png` |
| 3 | 世界背景设定页（当前对话级） | 通过 | `12-world.png` |
| 4 | API 接入页：接口、模型选择、推理等级、采样参数 | 通过 | `13-api.png` |
| 5 | 全局用户设定页 | 通过 | `14-user.png` |
| 6 | 酒馆人物管理：列表 + 当前标记 + 创建 | 通过 | `15-characters.png` |
| 7 | 角色卡编辑：人物名称 / 人物设定 / 人物外在形象 三要素 | 通过 | `16-character-edit.png` |
| 8 | 长按气泡弹出的「适中」操作栏：复制 / 修改 / 思维链 / 删除 | 通过 | `17-longpress-menu.png` |
| 9 | 点击气泡改内容，并提示「之后 N 条会被清除」 | 通过 | `18-edit-message.png` |
| 10 | 单条消息思维链开关（开 → 关，色块收起） | 通过 | `24-scroll.png`（开）→ `26-reasoning-verified.png`（关） |

静态检查：`flutter analyze` **No issues found**；`flutter test` **7/7 通过**。

产物：
- `app/build/app/outputs/flutter-apk/app-debug.apk` — 调试包，含 4 种 ABI
- `app/build/app/outputs/flutter-apk/app-release.apk` — 发布包（debug 签名），适合装真机

> 注：截图中的对话为注入的示例数据（`tools/seed.js`），用于验证版式；API Key 为空，
> 因此底部显示「未配置 API」。填入真实 Key 后即可实际对话。

